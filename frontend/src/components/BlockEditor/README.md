# BlockEditor Component

Блочный редактор контента для образовательного портала с поддержкой различных типов контента.

## Возможности

- 📝 **Форматированный текст** - WYSIWYG редактор на основе Tiptap
- 🎥 **Видео** - Встраивание YouTube и Vimeo
- 🖼 **Изображения** - Загрузка с drag & drop
- 🔗 **Ссылки** - Внешние и внутренние ссылки
- 📋 **Домашние задания** - Конфигурация заданий с дедлайнами
- 🎯 **Drag & Drop** - Изменение порядка блоков перетаскиванием
- 👁 **Предпросмотр** - Просмотр контента как видят студенты
- 💾 **Автосохранение** - Отслеживание изменений

## Структура компонентов

```
BlockEditor/
├── BlockEditor.js          # Главный компонент
├── BlockEditor.css         # Стили
├── BlockList.js            # Список блоков
├── BlockItem.js            # Обёртка блока с DnD
├── BlockToolbar.js         # Панель добавления блоков
├── blocks/                 # Типы блоков
│   ├── TextBlock.js
│   ├── VideoBlock.js
│   ├── ImageBlock.js
│   ├── LinkBlock.js
│   └── HomeworkBlock.js
├── preview/                # Предпросмотр
│   └── BlockPreview.js
└── utils/                  # Утилиты
    └── videoUtils.js
```

## Установленные зависимости

```json
{
  "@tiptap/react": "^2.x",
  "@tiptap/starter-kit": "^2.x",
  "@tiptap/extension-link": "^2.x",
  "@tiptap/extension-image": "^2.x",
  "@tiptap/extension-placeholder": "^2.x",
  "react-dnd": "^16.x",
  "react-dnd-html5-backend": "^16.x"
}
```

## Быстрый старт

```jsx
import BlockEditor from './components/BlockEditor';

function MyEditor() {
  const [blocks, setBlocks] = useState([]);

  return (
    <BlockEditor
      blocks={blocks}
      onBlocksChange={setBlocks}
    />
  );
}
```

## API Methods (services/api.js)

Добавлены следующие методы в `coursesAPI`:

```javascript
// Загрузка изображения
uploadImage: (file, sectionId) => FormData

// Изменение порядка элементов
reorderElements: (items) => Promise
```

## Типы блоков

### TextBlock
WYSIWYG редактор с форматированием:
- Bold, Italic
- Headings (H2, H3)
- Lists (bullet, ordered)
- Blockquotes
- Links
- Images
- Undo/Redo

### VideoBlock
Поддержка:
- YouTube (все форматы URL)
- Vimeo (все форматы URL)
- Автоматический парсинг и валидация
- Preview с iframe embed

### ImageBlock
- Загрузка через клик или drag & drop
- Валидация типа и размера файла
- Preview загруженного изображения
- Поля: alt text, caption
- Максимальный размер: 10MB

### LinkBlock
- Валидация URL
- Кастомный текст ссылки
- Опция "Открывать в новой вкладке"
- Preview ссылки

### HomeworkBlock
Конфигурация задания:
- Описание (textarea)
- Дедлайн (datetime-local)
- Допустимые форматы файлов (checkboxes)
- Максимальный размер файла (1-100 MB)

## Особенности реализации

### Drag & Drop
- Использует react-dnd + HTML5Backend
- Плавная анимация перетаскивания
- Визуальная индикация drop zone
- Автоматическое обновление порядка

### Tiptap Editor
- Расширяемая архитектура
- Поддержка markdown shortcuts
- Сохранение в HTML и JSON
- Placeholder текст

### State Management
- Контроллируемый компонент
- Callback onBlocksChange при любом изменении
- Локальный state для UI (preview mode)

### Responsive Design
- Адаптивная toolbar
- Мобильные оптимизации
- Touch-friendly интерфейс

## Стилизация

Компонент использует:
- Bootstrap 5 классы (кнопки, формы, badges)
- Кастомный CSS в BlockEditor.css
- CSS переменные для кастомизации
- Адаптивные media queries

## Accessibility

- Семантический HTML
- ARIA атрибуты
- Keyboard navigation
- Alt текст для изображений
- Form labels

## Browser Support

- Chrome/Edge (последние 2 версии)
- Firefox (последние 2 версии)
- Safari (последние 2 версии)

## Примеры

Смотрите файл `USAGE_EXAMPLE.md` для детальных примеров интеграции.

## Лицензия

Часть образовательного портала portal_summer
