Сейчас текущий функционал добавление контента в курс желает лучшего.
Давай улучшим это. Прочитай функционал, который я хотел бы видеть. Предложи свои варианты его реализации.
Я предлагаю использовать готовый WYSIWYG-редактор + JSON-структуру

 1. Общая концепция
Редактор работает внутри раздела курса (например, “Введение в программирование”).
Контент состоит из блоков — отдельных элементов разного типа  в разделе
Блоки можно добавлять, редактировать, удалять, перетаскивать для изменения порядка.
Вся информация хранится в JSON-поле в Django модели.
Фронтенд реализован на React с использованием Tiptap (для текста), react-dnd (для перетаскивания) и Bootstrap (для стилей).

2. Функционал редактора (Frontend - React)
🖥️ Интерфейс:
A. Основной экран редактора:
Заголовок: «Редактирование раздела: [Название раздела]»
Кнопка «Добавить блок» → выпадающее меню с типами: Текст, Видео, Ссылка, ДЗ, Изображение.
Список блоков (в порядке order) — каждый блок отображается как карточка с:
Иконкой типа (📝, ▶️, 🔗, 📝, 🖼️)
Названием/предпросмотром контента
Кнопками: ✏️ Редактировать, 🗑️ Удалить, 🔄 Перетащить (drag handle)
Кнопка «Сохранить» внизу.
B. Редактирование блока:
При нажатии «Редактировать» — открывается модальное окно (или inline-редактор).
Для каждого типа — свой набор полей:
Текст: WYSIWYG-редактор (Tiptap) + поле заголовка.
Видео: поле URL + настройки ширины/высоты/autoplay.
Ссылка: URL + метка кнопки + открытие в новой вкладке.
ДЗ: текст вопроса, дедлайн (date-time picker), макс. баллы, инструкции, возможность загрузки файла.
Изображение: поле URL или загрузка файла (если поддерживается).
C. Перетаскивание блоков:
Используется react-dnd или @dnd-kit/core.
При перетаскивании — меняется order у блоков.
После отпускания — обновляется список и сохраняется в state.
D. Предпросмотр:
Кнопка «Предпросмотр» → рендерит контент так, как он будет выглядеть для студента.
Можно показывать в отдельном табе или модальном окне.
E. Валидация:
Обязательные поля: content, type, order.
Для видео — валидация URL (начинается с https:// и содержит youtube.com или vimeo.com).
Для ДЗ — дедлайн не может быть в прошлом.